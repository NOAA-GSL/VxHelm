apiVersion: v1
data:
  mongo-init.js: |
    db.createUser(
            {
                user: "{{ .Values.matsUser }}",
                pwd: "{{ .Values.matsPassword }}",
                db: "admin"
                roles: [
                    {
                        role: "userAdminAnyDatabase",
                        db: "admin"
                    }
                ]
            }
    );
kind: ConfigMap
metadata:
  name: mongo-init
  namespace: matsdev
