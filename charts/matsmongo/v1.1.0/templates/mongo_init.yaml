apiVersion: v1
data:
  mongo-init.js: |
    db.createUser(
            {
                user: "{{ .Values.matsMongoUser }}",
                pwd: "{{ .Values.matsMongoPassword }}",
                roles: [
                    {
                        role: "root",
                        db: "admin"
                    },
                    {
                        role: "root",
                        db: "admin"
                    },
                    {
                        role: "root",
                        db: "admin"
                    }
                ]
            }
    );
kind: ConfigMap
metadata:
  name: mongo-init
  namespace: matsdev
