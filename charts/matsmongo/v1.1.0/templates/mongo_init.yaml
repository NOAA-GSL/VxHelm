apiVersion: v1
data:
  mongo-init.js: |
    db.createUser(
            {
                user: "{{ .Values.matsMongoUser }}",
                pwd: "{{ .Values.matsMongoPassword }}",
                roles: [
                    {
                        role: "root",
                        db: "aircraft"
                    },
                    {
                        role: "root",
                        db: "aircraft"
                    },
                    {
                        role: "root",
                        db: "aircraft"
                    }
                ]
            }
    );
kind: ConfigMap
metadata:
  name: mongo-init
  namespace: matsdev
