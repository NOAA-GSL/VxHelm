apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: met-airquality
  namespace: {{ .Values.deploymentEnvironment }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Values.deploymentEnvironment }}/met-airquality"
            backend:
              serviceName: met-airquality
              servicePort: 80
