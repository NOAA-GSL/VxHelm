apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ceiling15
  namespace: {{ .Values.deploymentEnvironment }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Values.deploymentEnvironment }}/ceiling15"
            backend:
              serviceName: ceiling15
              servicePort: 80
