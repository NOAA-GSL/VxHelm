apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: precipaqpi
  namespace: {{ .Release.Namespace }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Release.Namespace }}/precipaqpi"
            backend:
              serviceName: precipaqpi
              servicePort: 80
