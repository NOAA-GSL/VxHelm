apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ceiling
  namespace: {{ .Values.deploymentEnvironment }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Values.deploymentEnvironment }}/ceiling"
            backend:
              serviceName: ceiling
              servicePort: 80
