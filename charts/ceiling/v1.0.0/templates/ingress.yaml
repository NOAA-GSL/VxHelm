apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ceiling
  namespace: {{ .Release.Namespace }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Release.Namespace }}/ceiling"
            backend:
              serviceName: ceiling
              servicePort: 80
