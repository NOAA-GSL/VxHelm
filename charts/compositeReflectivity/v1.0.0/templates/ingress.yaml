apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: compositeReflectivity
  namespace: {{ .Release.Namespace }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Release.Namespace }}/compositeReflectivity"
            backend:
              serviceName: compositeReflectivity
              servicePort: 80
