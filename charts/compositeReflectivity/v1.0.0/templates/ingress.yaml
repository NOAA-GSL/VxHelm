apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: compositeReflectivity
  namespace: {{ .Values.deploymentEnvironment }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Values.deploymentEnvironment }}/compositeReflectivity"
            backend:
              serviceName: compositeReflectivity
              servicePort: 80
