categories:
  - MATSAPP
questions:
- variable: deploymentEnvironment
  default: "matsprod"
  description: "The deployment environment one of [matsdev, matspreint, matsint, matsprod]"
  type: enum
  options: ["matsdev","matspreint","matsint","matsprod"]
  required: true
  label: The Deployment Environment MUST MATCH MATSMONGO AND MATSHOME
- variable: rootUrl
  default: https://rancher.localhost
  type: string
  required: true
  label: The root URL of this deployment
- variable: defaultImage
  default: "true"
  description: "Use default app version for Docker image tag."
  label: Use Default App version
  type: boolean
  show_subquestion_if: false
  group: "Container Images"
  subquestions:
   - variable: image.appVersion
     default: "2.3.13"
     description: "app version is used to create the app docker image tag"
     type: string
     label: App Version
- variable: persistentVolumeClaim
  default: "matsdata"
  description: "The preconfigured persistent volume claim"
  type: pvc
  required: true
  label: The persistent Volume Claim where the app data will be persisted - MUST MATCH MATSMONGO AND MATSHOME
