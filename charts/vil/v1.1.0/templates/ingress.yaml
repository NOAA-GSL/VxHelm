apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: vil
  namespace: {{ .Release.Namespace }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Release.Namespace }}/vil"
            backend:
              serviceName: vil
              servicePort: 80
