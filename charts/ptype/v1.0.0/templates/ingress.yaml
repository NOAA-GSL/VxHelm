apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ptype
  namespace: {{ .Values.deploymentEnvironment }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Values.deploymentEnvironment }}/ptype"
            backend:
              serviceName: ptype
              servicePort: 80
