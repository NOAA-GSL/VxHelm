apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: visibility15
  namespace: {{ .Values.deploymentEnvironment }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Values.deploymentEnvironment }}/visibility15"
            backend:
              serviceName: visibility15
              servicePort: 80
