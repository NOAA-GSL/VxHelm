apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: visibility
  namespace: {{ .Release.Namespace }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Release.Namespace }}/visibility"
            backend:
              serviceName: visibility
              servicePort: 80
