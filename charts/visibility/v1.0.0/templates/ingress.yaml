apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: visibility
  namespace: {{ .Values.deploymentEnvironment }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Values.deploymentEnvironment }}/visibility"
            backend:
              serviceName: visibility
              servicePort: 80
