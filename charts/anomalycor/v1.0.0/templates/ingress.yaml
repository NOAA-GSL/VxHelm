apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: anomalycor
  namespace: {{ .Values.deploymentEnvironment }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Values.deploymentEnvironment }}/anomalycor"
            backend:
              serviceName: anomalycor
              servicePort: 80
