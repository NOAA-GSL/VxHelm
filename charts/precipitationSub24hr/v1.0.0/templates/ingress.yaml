apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: precipitationSub24hr
  namespace: {{ .Release.Namespace }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Release.Namespace }}/precipitationSub24hr"
            backend:
              serviceName: precipitationSub24hr
              servicePort: 80
