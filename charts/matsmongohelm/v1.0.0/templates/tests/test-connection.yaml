apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "matsmongohelm.fullname" . }}-test-connection"
  labels:
{{ include "matsmongohelm.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "matsmongohelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
