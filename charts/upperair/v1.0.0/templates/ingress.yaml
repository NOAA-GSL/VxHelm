apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: upperair
  namespace: {{ .Values.deploymentEnvironment }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Values.deploymentEnvironment }}/upperair"
            backend:
              serviceName: upperair
              servicePort: 80
