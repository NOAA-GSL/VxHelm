# Default values for matsmongo.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
persistentVolumeClaim: matsdata
rootUrl: https://rancher.localhost
userId: ""
mongoUser: "matsUser"
mongoPwd: "mu-2019"
image:
  repository: matsapps/production
  appVersion: 2.3.13
  pullPolicy: IfNotPresent

