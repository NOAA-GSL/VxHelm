apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: surfrad
  namespace: {{ .Values.deploymentEnvironment }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Values.deploymentEnvironment }}/surfrad"
            backend:
              serviceName: surfrad
              servicePort: 80
