apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: surfrad
  namespace: {{ .Release.Namespace }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Release.Namespace }}/surfrad"
            backend:
              serviceName: surfrad
              servicePort: 80
