apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: precipitation24hr
  namespace: {{ .Values.deploymentEnvironment }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Values.deploymentEnvironment }}/precipitation24hr"
            backend:
              serviceName: precipitation24hr
              servicePort: 80
