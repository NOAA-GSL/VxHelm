apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: echotop
  namespace: {{ .Values.deploymentEnvironment }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Values.deploymentEnvironment }}/echotop"
            backend:
              serviceName: echotop
              servicePort: 80
