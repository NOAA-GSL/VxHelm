apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: met-upperair
  namespace: {{ .Values.deploymentEnvironment }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Values.deploymentEnvironment }}/met-upperair"
            backend:
              serviceName: met-upperair
              servicePort: 80
