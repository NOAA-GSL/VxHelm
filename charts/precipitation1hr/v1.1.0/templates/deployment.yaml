apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
    field.cattle.io/creatorId: {{ .Values.userId }}
    field.cattle.io/publicEndpoints: '[{"addresses":[""],"port":80,"protocol":"HTTP","serviceName":"{{ .Release.Namespace }}:precipitation1hr","ingressName":"{{ .Release.Namespace }}:matsapps-{{ .Release.Namespace }}","hostname":"{{ .Values.rootUrl }}","path":"/{{ .Release.Namespace }}/precipitation1hr","allNodes":false},{"protocol":"TCP","serviceName":"{{ .Release.Namespace }}:precipitation1hr-nodeport","allNodes":true}]'
  labels:
    cattle.io/creator: norman
    workload.user.cattle.io/workloadselector: "deployment-{{ .Release.Namespace }}-precipitation1hr"
  name: precipitation1hr
  selfLink: "/apis/apps/v1/namespaces/{{ .Release.Namespace }}/deployments/precipitation1hr"
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      workload.user.cattle.io/workloadselector: "deployment-{{ .Release.Namespace }}-precipitation1hr"
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      annotations:
        field.cattle.io/ports: '[[{"containerPort":80,"dnsName":"precipitation1hr-nodeport","kind":"NodePort","name":"80tcp01","protocol":"TCP"}]]'
      labels:
        workload.user.cattle.io/workloadselector: "deployment-{{ .Release.Namespace }}-precipitation1hr"
    spec:
      containers:
        - env:
            - name: MONOG_URL
              value: "mongodb://{{ .Values.matsMongoUser }}:{{ .Values.matsMongoPassword }}@localhost:27017"
            - name: DEPLOYMENT_ENVIRONMENT
              {{- if eq .Release.Namespace "matsdev" }}
              value: "development"
              {{- else if eq .Release.Namespace "matspreint" }}
              value: "integration"
              {{- else if eq .Release.Namespace "matsint" }}
              value: "production"
              {{- else if eq .Release.Namespace "matsprod" }}
              value: "production"
              {{- end }}
            - name: GROUP_ORDER
              value: Upper Air,Ceiling and Visibility,Surface,Precipitation,Radar,METexpress
            - name: ROOT_URL
              value: "{{ .Values.rootUrl }}/{{ .Release.Namespace }}/precipitation1hr"
          {{- if eq .Release.Namespace "matsdev" }}
          image: "matsapps/development:precipitation1hr-{{ .Values.image.appVersion }}"
          {{- else if eq .Release.Namespace "matspreint" }}
          image: "matsapps/integration:precipitation1hr-{{ .Values.image.appVersion }}"
          {{- else if eq .Release.Namespace "matsint" }}
          image: "matsapps/production:precipitation1hr-{{ .Values.image.appVersion }}"
          {{- else if eq .Release.Namespace "matsprod" }}
          image: "matsapps/production:precipitation1hr-{{ .Values.image.appVersion }}"
          {{- end }}
          imagePullPolicy: Always
          name: precipitation1hr
          ports:
            - containerPort: 80
              name: 80tcp01
              protocol: TCP
          resources: {}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: {}
            privileged: false
            readOnlyRootFilesystem: false
            runAsNonRoot: false
          stdin: true
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          tty: true
          volumeMounts:
            - mountPath: /usr/app/settings
              name: settings
              subPath: {{ .Release.Namespace }}/settings
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
      volumes:
        - name: settings
          persistentVolumeClaim:
            claimName: matsdata
