apiVersion: v1
data:
  {{ .Values.appName }}-init.js: |
    db.createUser(
            {
                user: "{{ .Values.matsMongoUser }}",
                pwd: "{{ .Values.matsMongoPassword }}",
                roles: [
                    {
                        role: "userAdminAnyDatabase",
                        db: "admin"
                    },
                    {
                        role: "dbAdminAnyDatabase",
                        db: "admin"
                    },
                    {
                        role: "readWriteAnyDatabase",
                        db: "admin"
                    }
                ]
            }
    );
kind: ConfigMap
metadata:
  name: {{ .Values.appName }}-init
  namespace: {{ .Release.Namespace }}
