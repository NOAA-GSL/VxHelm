apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: landuse
  namespace: {{ .Release.Namespace }}
spec:
  rules:
    - host: {{ .Values.rootUrl | replace "https://" "" }}
      http:
        paths:
          - path: "/{{ .Release.Namespace }}/landuse"
            backend:
              serviceName: landuse
              servicePort: 80
